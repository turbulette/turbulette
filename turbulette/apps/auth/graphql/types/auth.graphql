directive @scope(permissions: [String!]) on FIELD_DEFINITION
directive @login_required on FIELD_DEFINITION
directive @staff_member_required on FIELD_DEFINITION

type JsonWebToken {
  accessToken: String
  refreshToken: String
  errors: [String]
}

type AccessToken {
  accessToken: String
  errors: [String]
}
